apiVersion: v1
kind: Service
metadata:
  name: kibana
  namespace: efk-stack
  labels:
    app: kibana
spec:
  selector:
    app: kibana
  ports:
  - port: 5601
    targetPort: 5601
    protocol: TCP
    name: http
  type: LoadBalancer 